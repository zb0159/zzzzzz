#include "log.h"
void p_log(const char *str, ...)
{
    /**va_list ap;
    FILE *fh = NULL;
    if((fh = fopen(COMMLIB_DBG_FILE, "a")))
    {
        va_start(ap, str);
        vfprintf(fh, str, ap);
        fprintf(fh, "\n");
        va_end(ap);
        fclose(fh);
        fh = NULL;
    }**/
}
void LOG(const char *str, ...)
{
}

